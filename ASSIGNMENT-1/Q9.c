#include<stdio.h>
int main (void)
{
    float celsius,fahrenite;
    printf("Enter the temprature in celsius: ");
    scanf("%f",&celsius);

    celsius = 5/9 * fahrenite - 32;
    printf("%0.2f celsius = %0.2f fahrenite",celsius, fahrenite);
    return 0;
}